#include<iostream>
#include<stdio.h>
#include"geometry.h"

using namespace std;

// template <typename T> struct TYPE {
//     typedef array<pair<string, T>, 2> _SINGLE_VALUES;
//     typedef array<pair<string, array<T, 2>>, 2> _ARRAY;
// };

// template <typename inputtype,typename outputtype> struct SYSTEM {
//     typedef TYPE<inputtype> _TYPE;
//     _TYPE::_SINGLE_VALUES k;
//     // typedef map< string, inputtype> _input_type;
//     // typedef map< string, outputtype> _output_type; 
// };